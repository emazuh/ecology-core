name: birds_layer_search
task: layer_search

datasets:
  - name: birds_inat
    fractions: [0.05, 0.1, 0.25]

models:
  - mobilevit
  - efficientformer

adapter:
  chain_type: par_fixed
  config: null

hpo:
  n_trials: 10
  epochs: 20
  objective: accuracy
  pruner:
    type: percentile
    percentile: 15
    warmup_steps: 5

logging:
  wandb: true
  project: ecology-vision-moe
  entity: ecology-multimodal-2026

seeds: [0]
